version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next  # Directory where SSR build artifacts are generated
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
